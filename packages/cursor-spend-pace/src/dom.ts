/** @deprecated Import from `./dom/shared` or `./platform` instead. */
export {
  dashboardPath,
  findFill,
  isSpendingPage,
  isUsageTrack,
  parseUsedFromFill,
  PRO_TRACK_SELECTOR as TRACK_SELECTOR,
  queryUsageTracks,
} from './dom/shared';

export { findGrokTracks as grokTracks } from './platform/pro/tracks';
